name: iOS Unsigned IPA Build

on:
  workflow_dispatch:

jobs:
  build-ios:
    runs-on: macos-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Install CocoaPods
        run: sudo gem install cocoapods

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Build iOS Frameworks
        run: |
          ./gradlew iosArm64Binaries
          ./gradlew iosSimulatorArm64Binaries

      - name: Create Xcode Project and Build IPA
        run: |
          # Beispiel: passe den Pfad und den Namen an dein Projekt an
          xcodebuild \
            -project iosApp/iosApp.xcodeproj \
            -scheme iosApp \
            -configuration Release \
            -sdk iphoneos \
            -archivePath build/iosApp.xcarchive archive || true

          mkdir -p build/ipa
          # Exportiere ein unsigniertes IPA
          xcodebuild \
            -exportArchive \
            -archivePath build/iosApp.xcarchive \
            -exportPath build/ipa \
            -exportOptionsPlist iosApp/exportOptions.plist || true

      - name: Upload Unsigned IPA
        uses: actions/upload-artifact@v4
        with:
          name: Unsigned-iOS-App
          path: build/ipa
